import { signUpSchema, TSingUpType } from "./register.validation";

import { loginSchema, TLoginType } from "./login.validation";

export { signUpSchema, loginSchema, type TLoginType, type TSingUpType };
